#include "main.h"

/**
 * _isupper -> a function that checks upper case character
 * @c: an integer paramer
 * Return: anything but success
 */
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
